import { UserValidationRequest } from './user-validation-request';

describe('UserValidationRequest', () => {
  it('should create an instance', () => {
    expect(new UserValidationRequest()).toBeTruthy();
  });
});
